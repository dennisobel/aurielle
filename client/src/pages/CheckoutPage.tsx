import { useState } from "react";
import { ArrowLeft, ArrowRight, Check, LockKeyhole, MapPin } from "lucide-react";
import { Link } from "wouter";
import StoreHeader from "@/components/StoreHeader";
import { useCart } from "@/contexts/CartContext";

const money = (value: number) => `$${value.toLocaleString("en-US")}`;

export default function CheckoutPage() {
  const { items, subtotal, shipping, total, clearCart } = useCart();
  const [completed, setCompleted] = useState(false);
  if (completed) return <div className="min-h-screen bg-[#fbfaf7] text-[#181716]"><StoreHeader minimal /><main className="checkout-success"><div className="success-mark"><Check size={27} /></div><p className="eyebrow">Thank you for choosing us</p><h1>Your pieces are<br /><em>on their way.</em></h1><p>Order RA–2026–0408 is confirmed. We’ll send a note to your inbox as soon as your package leaves our atelier.</p><Link href="/" className="button-primary">Return to the collection <ArrowRight size={15} /></Link></main></div>;
  return <div className="min-h-screen bg-[#fbfaf7] text-[#181716]"><StoreHeader minimal /><main className="container checkout-page">{items.length === 0 ? <div className="checkout-empty"><h1>Your bag is empty.</h1><Link href="/" className="button-primary">Return to collection <ArrowRight size={15} /></Link></div> : <div className="checkout-layout"><section className="checkout-form"><Link href="/cart" className="back-link"><ArrowLeft size={15} /> Back to bag</Link><div className="checkout-title"><p className="eyebrow">Almost yours</p><h1>Complete your order.</h1><p>We keep checkout simple, secure, and considered.</p></div><form onSubmit={(event) => { event.preventDefault(); clearCart(); setCompleted(true); }}><div className="checkout-section"><div className="checkout-section-heading"><span>01</span><div><h2>Contact</h2><p>We’ll use this to send your order updates.</p></div></div><label>Email address<input type="email" placeholder="you@example.com" required /></label><label className="checkbox-row"><input type="checkbox" /><span className="fake-checkbox" />Email me with notes from the atelier</label></div><div className="checkout-section"><div className="checkout-section-heading"><span>02</span><div><h2>Delivery</h2><p>Where should we send your piece?</p></div></div><div className="form-grid"><label>First name<input required placeholder="First name" /></label><label>Last name<input required placeholder="Last name" /></label></div><label>Address<input required placeholder="Street address" /></label><div className="form-grid three"><label>City<input required placeholder="City" /></label><label>Region<select required defaultValue=""><option value="" disabled>Region</option><option>Ontario</option><option>California</option><option>New York</option></select></label><label>Postal code<input required placeholder="Postal code" /></label></div><div className="delivery-note"><MapPin size={16} /><span><strong>Complimentary standard delivery</strong>Estimated arrival in 2–4 business days.</span></div></div><div className="checkout-section"><div className="checkout-section-heading"><span>03</span><div><h2>Payment</h2><p>Securely processed. You won’t be charged yet in this demo.</p></div></div><label>Card number<input required inputMode="numeric" placeholder="1234 1234 1234 1234" /></label><div className="form-grid"><label>Expiry date<input required placeholder="MM / YY" /></label><label>Security code<input required placeholder="CVC" /></label></div></div><button className="button-primary checkout-submit" type="submit"><LockKeyhole size={15} /> Place order · {money(total)} <ArrowRight size={15} /></button></form></section><aside className="checkout-summary"><p className="eyebrow">In your bag</p><h2>Order summary</h2><div className="checkout-items">{items.map((item) => <div key={item.id}><div className="checkout-item-image"><img src={item.image} alt={item.name} /><span>{item.quantity}</span></div><div><strong>{item.name}</strong><p>{item.metal}</p></div><b>{money(item.price * item.quantity)}</b></div>)}</div><div className="summary-rows"><div><span>Subtotal</span><strong>{money(subtotal)}</strong></div><div><span>Shipping</span><strong>{shipping === 0 ? "Complimentary" : money(shipping)}</strong></div><div className="summary-total"><span>Total</span><strong>{money(total)}</strong></div></div><p className="checkout-trust">✦ Your payment details are encrypted and protected.</p></aside></div>}</main></div>;
}
